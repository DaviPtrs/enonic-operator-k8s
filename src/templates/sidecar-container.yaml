name: enonic-sidecar
image: daviptrs/enonic-operator-k8s-sidecar:latest
imagePullPolicy: Always
env:
  - name: DEBUG
    value: "{debug}"
  - name: ENONIC_AUTH
    valueFrom:
      secretKeyRef:
        key: auth
        name: "{name}-auth"
volumeMounts:
  - mountPath: /exit
    name: exit-folder
